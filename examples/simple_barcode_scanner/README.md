# Barcode Scanner in JavaScript
A modern web application that uses Dynamsoft Capture Vision to scan **1D/2D barcodes** within a web browser. This implementation uses the native `getUserMedia` API for camera access and features a clean, responsive user interface.

## Features
- 📷 **Real-time barcode scanning** using native browser camera access
- 🎯 **Visual feedback** with bounding box overlay on detected barcodes
- 📱 **Responsive design** that works on desktop and mobile devices
- 🎨 **Modern UI** with gradient theme and smooth animations
- 📁 **File upload support** for scanning barcodes from images
- 🔄 **Multiple camera support** with device selection

## Prerequisites
- Obtain a [30-day free trial license](https://www.dynamsoft.com/customer/license/trialLicense/?product=dcv&package=cross-platform) for Dynamsoft Capture Vision.


## How to Run

```bash
python -m http.server
```

Open your web browser and navigate to http://localhost:8000 to access the application.




